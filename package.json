{
  "name": "cypresstask",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
     "cy:chromeHeaded":"cypress run --browser chrome --headed",
"cy:chromeHeadless":"cypress run --browser chrome"

    
  },
  "author": "Latika",
  "license": "ISC",
  "dependencies": {
    "@cypress/browserify-preprocessor": "^3.0.2",
    "@cypress/xpath": "^2.0.3",
    "@types/cypress-cucumber-preprocessor": "^4.0.1",
    "cypress": "^12.7.0",
    "cypress-cucumber-preprocessor": "^4.3.1"
  },
  "cypress-cucumber-preprocessor": {
    "nonGlobalStepDefinitions": true,
    "cucumberJson": {
      "generate": true,
      "outputFolder": "cypress/cucumber-json",
      "filePrefix": "",
      "fileSuffix": ".cucumber"
    }
  },
  "devDependencies": {
    "cypress-xpath": "^2.0.1",
    "multiple-cucumber-html-reporter": "^3.2.0"
  }
}
